<% include header %>
<h2>Account</h2>
<p>
    <% if (user) { %>
        Logged in as <b><%= user.name %></b>. <a href="/logout">Logout</a>
    <% } else { %>
        Not logged in. <a href="/login">Login</a>
    <% } %>
</p>
<% include footer %>
